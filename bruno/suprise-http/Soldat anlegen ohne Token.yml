info:
  name: Soldat anlegen ohne Token
  type: http
  seq: 7

http:
  method: POST
  url: "{{restUrl}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "vorname": "Eren",
        "nachname": "Jaeger",
        "geburtsdatum": "2000-01-01",
        "geschlecht": "MAENNLICH",
        "rang": "SOLDAT",
        "username": "{{soldatUsername}}",
        "ausruestung": {
          "waffe": "ODM_GEAR",
          "seriennummer": "{{soldatSeriennummer}}"
        }
      }
  auth: none

runtime:
  scripts:
    - type: pre-request
      code: |-
        const suffix = Date.now().toString();
        bru.setVar('soldatUsername', 'eren-' + suffix);
        bru.setVar('soldatSeriennummer', 'AOT-' + suffix);
  assertions:
    - expression: res.status
      operator: eq
      value: "401"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
